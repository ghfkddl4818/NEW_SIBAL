[pytest]
addopts = -ra
asyncio_mode = auto
testpaths = tests
markers =
    e2e: marks tests that drive external browsers or services (deselect with '-m "not e2e"')
    smoke: lightweight integration checks
