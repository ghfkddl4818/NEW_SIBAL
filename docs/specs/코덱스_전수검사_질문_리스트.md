# 🤖 코덱스 전수검사 질문 리스트

## 📋 코덱스에게 물어볼 질문들

### 🌐 웹 자동화 관련 질문
```
1. ultimate_automation_system.py의 execute_web_automation() 메서드가 실제로 pyautogui로 마우스를 움직이는지 확인해줘. 더미 루프만 돌고 있는 것 같은데?

2. process_single_product() 메서드에서 실제 이미지 인식(locateOnScreen)과 클릭 동작이 일어나는지 체크해줘.

3. expand_product_details() 메서드가 16번 스크롤하면서 detail_button.png를 찾는 로직이 제대로 구현되어 있는지 확인해줘.

4. 웹 자동화가 30개를 순식간에 처리했다고 나오는데, 실제로는 아무것도 안 하고 있어. 어디서 가짜 성공 처리하고 있는지 찾아줘.
```

### 🤖 AI 콜드메일 생성 관련 질문
```
5. llm/gemini_client.py에서 실제 Gemini API 호출이 일어나는지 확인해줘. API 키와 환경변수 설정이 맞는지도.

6. OCR 처리 부분(ocr/engine.py)에서 tesseract가 제대로 작동하는지 체크해줘. 이미지에서 텍스트 추출이 실제로 되는지.

7. compose/composer.py에서 최종 이메일 생성 로직이 실제로 작동하는지 확인해줘.

8. AI 처리 후 outputs 폴더에 결과 파일들이 실제로 저장되는지 확인해줘.
```

### 👥 고객사 발굴 관련 질문
```
9. client_discovery/main_crawler.py가 실제로 네이버 쇼핑을 크롤링하는지 확인해줘.

10. 크롤링 결과가 CSV 파일로 제대로 저장되는지 체크해줘.

11. 차단 업체 리스트 필터링이 제대로 작동하는지 확인해줘.
```

### 📁 파일 정리 시스템 관련 질문
```
12. FIFO 큐 로직(get_next_pending_store, clean_old_pending_stores)이 실제로 작동하는지 확인해줘.

13. 파일 해시 중복 체크(get_file_hash, is_duplicate_file)가 제대로 구현되어 있는지 체크해줘.

14. 데이터베이스 관리 함수들(add_to_database, update_database_entry)이 실제 Excel 파일을 수정하는지 확인해줘.
```

### 🖥️ GUI 및 통합 관련 질문
```
15. GUI 탭 전환과 버튼 클릭이 제대로 작동하는지 확인해줘.

16. 진행률 표시바와 상태 업데이트가 실시간으로 되는지 체크해줘.

17. 각 모듈 간 연결(웹자동화 → AI처리 → 파일정리)이 실제로 데이터를 주고받는지 확인해줘.

18. 로그 시스템이 모든 동작을 제대로 기록하는지 확인해줘.
```

### ⚙️ 설정 및 환경 관련 질문
```
19. config/config.yaml과 client_discovery/config.json의 설정값들이 실제 코드에서 읽히고 적용되는지 확인해줘.

20. 환경변수 VERTEX_PROJECT_ID가 제대로 인식되는지 체크해줘.

21. assets/img/ 폴더의 이미지 파일들이 실제 pyautogui에서 인식되는지 확인해줘.

22. requirements.txt의 모든 패키지가 설치되어 있고 import 오류가 없는지 체크해줘.
```

### 🔒 안전 모니터링 관련 질문
```
23. core/safety_monitor.py의 API 비용 추적과 메모리 모니터링이 실제로 작동하는지 확인해줘.

24. 응급 중단 기능이 제대로 구현되어 있고 작동하는지 체크해줘.

25. 백그라운드 안전 모니터링 스레드가 실제로 돌아가는지 확인해줘.
```

## 🎯 우선순위 질문 (가장 중요한 3개)
```
❗ 1순위: 웹 자동화가 왜 가짜로 30개 처리했다고 나오는지 (질문 1, 4)
❗ 2순위: 각 모듈이 실제로 작동하는지 아니면 더미 코드인지 (질문 5, 9, 12)
❗ 3순위: 모듈 간 데이터 연결이 실제로 되는지 (질문 17)
```

## 📝 코덱스에게 요청할 때 말할 내용
```
"이 프로젝트의 모든 기능을 전수검사해줘. 특히 웹 자동화가 실제로는 아무것도 안 하고 가짜로 성공했다고 나오는 문제가 있어. 위 질문들을 하나씩 체크해서 어떤 부분이 실제로 작동하고 어떤 부분이 더미 코드인지 알려줘. 그리고 각 문제점에 대한 해결책도 제시해줘."
```